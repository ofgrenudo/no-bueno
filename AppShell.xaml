<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="no_bueno.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:no_bueno">
    <TabBar>
       <Tab>
           <ShellContent Title="Bang" ContentTemplate="{DataTemplate local:MainPage}"/>
           <ShellContent Title="Settings" ContentTemplate="{DataTemplate local:SettingsPage}"/>
       </Tab>
    </TabBar>
</Shell>